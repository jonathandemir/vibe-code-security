name: Vibe Code Security Guard (Pro Engine)

on: [push]

jobs:
  semgrep-scan:
    name: Semgrep Security Analysis
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Code vom Nutzer herunterladen
        uses: actions/checkout@v4

      - name: 🚨 Semgrep Polyglot Scan
        uses: returntocorp/semgrep-action@v1
        with:
          # p/default lädt automatisch hunderte Sicherheitsregeln für Python, JS, TS, Go etc.
          config: "p/default"